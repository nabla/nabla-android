<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    >

    <ImageView
        android:id="@+id/conversationVideoMessageImageView"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:scaleType="centerInside"
        android:adjustViewBounds="true"
        android:maxHeight="300dp"
        app:layout_constrainedHeight="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:ignore="ContentDescription"
        />

    <ImageView
        android:id="@+id/conversationVideoMessagePlayIconImageView"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:contentDescription="@string/nabla_conversation_video_message_thumbnail_content_description"
        android:src="@drawable/ic_video_thumbnail_play"
        app:layout_constraintBottom_toBottomOf="@id/conversationVideoMessageImageView"
        app:layout_constraintEnd_toEndOf="@id/conversationVideoMessageImageView"
        app:layout_constraintStart_toStartOf="@id/conversationVideoMessageImageView"
        app:layout_constraintTop_toTopOf="@id/conversationVideoMessageImageView"
        />

    <ProgressBar
        android:id="@+id/conversationVideoMessageLoadingProgressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="20dp"
        android:paddingVertical="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

</androidx.constraintlayout.widget.ConstraintLayout>