// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    ext.buildConfig = [
            'compileSdk': 32,
            'minSdk'    : 23,
            'targetSdk' : 32,
            'jvmTarget' : JavaVersion.VERSION_11,
            'groupId'   : 'com.nabla.sdk',
    ]

    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }

    dependencies {
        classpath libs.android.pluginGradle
        classpath libs.kotlin.pluginGradle
        classpath libs.kotlin.serialization.pluginGradle
        classpath libs.apollo.pluginGradle
        classpath libs.ktlint.pluginGradle
    }
}

subprojects {
    apply plugin: "org.jlleitschuh.gradle.ktlint"

    ktlint {
        setVersion(libs.versions.ktlint.get())
    }

    tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).configureEach {
        kotlinOptions {
            allWarningsAsErrors = true
            jvmTarget = buildConfig.jvmTarget
            freeCompilerArgs += "-opt-in=kotlin.RequiresOptIn,kotlinx.coroutines.FlowPreview"
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}